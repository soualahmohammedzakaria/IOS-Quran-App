import { defaultStyles } from "@/styles"
import { View, Text } from "react-native"

const RecitersScreen = () => {
    return (
        <View style={defaultStyles.container}>
            <Text style={defaultStyles.text}>Reciters Screen</Text>
            
        </View>
    )
}

export default RecitersScreen